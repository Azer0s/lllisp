;; Test file for nested module paths and calls

;; Import modules
(def graphics (use "graphics.ll"))
(def utils (use "utils.ll"))

;; Call a function in a nested module hierarchy
(graphics/ui/button/render "Click Me" 100 200 150 50)

;; Call a utility function with a nested path
(utils/string/format/pad-left "Hello" 10 " ") 